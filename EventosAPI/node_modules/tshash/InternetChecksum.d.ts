import Hashing from './Hashing';
export declare class InternetChecksumming implements Hashing {
    protected sum: number;
    protected phase: number;
    reset(): void;
    update(buf: Uint8Array): void;
    digestAsUint16(): number;
    digest(): Uint8Array;
}
export default class InternetChecksum {
    static newHashing(): InternetChecksumming;
}
